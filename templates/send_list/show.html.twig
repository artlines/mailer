{% extends 'base.html.twig' %}

{% block title %}SendList{% endblock %}

{% block body %}
    <h1>SendList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ send_list.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ send_list.name }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ send_list.createdAt ? send_list.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Emails</th>
                <td>{{ send_list.emails }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('send_list_index') }}">back to list</a>

    <a href="{{ path('send_list_edit', {'id': send_list.id}) }}">edit</a>

    {{ include('send_list/_delete_form.html.twig') }}
{% endblock %}